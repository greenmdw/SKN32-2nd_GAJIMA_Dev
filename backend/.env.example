# 가지마 FastAPI 백엔드 환경변수 예시. 실제 비밀은 backend/.env(깃 제외)에 둔다.
PORT=8090
API_KEY=anchor-dev-key

# 운영 DB(MySQL). MYSQL_HOST 미설정 시 백엔드는 memory 데모 모드로 동작.
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=project2
MYSQL_PASSWORD=********
MYSQL_DATABASE=project2db

# 시뮬 로그 전용(외부, 옵션)
NEON_URL=
