# 가지마 운영 백엔드(FastAPI/Python, 19-2). 추론 없음 — 레지스트리·평가 ingest·차트 API·예측 로그.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2
mysql-connector-python>=9.1   # 운영 DB(.env MYSQL_* 설정 시). 미설정 시 memory 폴백.
# pandas>=2.2                 # eval_predictions.parquet 직접 파싱이 필요할 때만(현재 chart JSON 사용)

# model_inference/python_model_loader 용(19-2 §10). prep_*_v2.joblib 번들 로드.
# 현재 백엔드는 미로드(eval JSON + 제출 점수)로 동작 → 실추론 켤 때 필요.
imbalanced-learn>=0.12,<0.14
catboost>=1.2,<1.3
xgboost>=2.0,<3
lightgbm>=4.0,<5
