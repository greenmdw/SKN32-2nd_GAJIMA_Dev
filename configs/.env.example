# 운영 DB = MySQL (우리 서버). 복사 → configs/.env 후 값 채우기.
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=anchor
# ※ Neon(Postgres)은 별도 = simulation_site 로그 전용(여기 아님)
# NEON_URL=postgres://...   (simulation_site 에서만 사용)
